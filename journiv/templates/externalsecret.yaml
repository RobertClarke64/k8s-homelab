apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: journiv
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  refreshInterval: 10s

  target:
    name: journiv

  dataFrom:
      - find:
          name:
            regexp: journiv.*
