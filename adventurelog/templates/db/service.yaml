apiVersion: v1
kind: Service
metadata:
  name: {{ include "adventurelog.db.fullname" . }}
  labels:
    {{- include "adventurelog.db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.db.service.type }}
  ports:
    - port: {{ .Values.db.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "adventurelog.db.selectorLabels" . | nindent 4 }}
