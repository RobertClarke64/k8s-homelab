{{ range $key, $value := .Values.applications }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $key }}
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/RobertClarke64/k8s-homelab
    targetRevision: HEAD
    path: {{ default $value.path $key}}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ $value.namespace | default $key}}
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
{{ end }}
