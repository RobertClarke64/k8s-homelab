{{- if .Values.db.persistentVolume.create -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.db.persistentVolume.name}}
spec:
  storageClassName: {{ .Values.db.persistentVolume.storageClass }}
  {{- with .Values.db.persistentVolume.accessModes }}
  accessModes:
    {{- toYaml . | nindent 4}}
  {{- end }}
  {{- with .Values.db.persistentVolume.resources }}
  resources:
    {{- toYaml . | nindent 4}}
  {{- end }}
{{- end }}

{{- range .Values.db.persistentVolume.additionalPersistentVolumeClaims }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .name }}
spec:
  storageClassName: {{ .storageClass }}
  {{- with .accessModes }}
  accessModes:
    {{- toYaml . | nindent 4}}
  {{- end }}
  {{- with .resources }}
  resources:
    {{- toYaml . | nindent 4}}
  {{- end }}
{{- end }}
