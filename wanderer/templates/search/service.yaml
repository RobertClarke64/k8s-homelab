apiVersion: v1
kind: Service
metadata:
  name: {{ include "wanderer.search.fullname" . }}
  labels:
    {{- include "wanderer.search.labels" . | nindent 4 }}
spec:
  type: {{ .Values.search.service.type }}
  ports:
    - port: {{ .Values.search.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wanderer.search.selectorLabels" . | nindent 4 }}
