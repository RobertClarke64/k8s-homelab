apiVersion: v1
kind: Service
metadata:
  name: {{ include "wanderer.web.fullname" . }}
  labels:
    {{- include "wanderer.web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wanderer.web.selectorLabels" . | nindent 4 }}
