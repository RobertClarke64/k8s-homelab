apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: grafana
  namespace: grafana
  labels:
    dashboards: grafana
spec:
  external:
    url: https://grafana.robertclarke.co.uk
    apiKey:
      name: grafana-operator-auth
      key: SERVICE_ACCOUNT_TOKEN
