apiVersion: v1
kind: Service
metadata:
  name: {{ include "barassistant.search.fullname" . }}
  labels:
    {{- include "barassistant.search.labels" . | nindent 4 }}
spec:
  type: {{ .Values.search.service.type }}
  ports:
    - port: {{ .Values.search.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "barassistant.search.selectorLabels" . | nindent 4 }}
