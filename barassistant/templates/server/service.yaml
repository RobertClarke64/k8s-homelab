apiVersion: v1
kind: Service
metadata:
  name: {{ include "barassistant.server.fullname" . }}
  labels:
    {{- include "barassistant.server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.server.service.type }}
  ports:
    - port: {{ .Values.server.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "barassistant.server.selectorLabels" . | nindent 4 }}
