apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: discord-registration
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  refreshInterval: 10s

  target:
    name: discord-registration

  data:
    - secretKey: registration.yaml
      remoteRef:
        key: /matrix-bridges/mautrix-discord-registration
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: whatsapp-registration
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  refreshInterval: 10s

  target:
    name: whatsapp-registration

  data:
    - secretKey: registration.yaml
      remoteRef:
        key: /matrix-bridges/mautrix-whatsapp-registration
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: meta-registration
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  refreshInterval: 10s

  target:
    name: meta-registration

  data:
    - secretKey: registration.yaml
      remoteRef:
        key: /matrix-bridges/mautrix-meta-registration
