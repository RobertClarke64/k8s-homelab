apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tandoor
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  refreshInterval: 10s

  target:
    name: tandoor

  data:
    - secretKey: SECRET_KEY
      remoteRef:
        key: /tandoor/SECRET_KEY
