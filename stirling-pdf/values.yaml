
stirling-pdf-chart:
  ingress:
    enabled: true
    hosts:
      - name: pdf.robertclarke.co.uk
    ingressClassName: "nginx"
    annotations:
      # proxy-body-size is set to 0 to remove the body limit on file uploads
      nginx.ingress.kubernetes.io/proxy-body-size: "0"
