{{- range $service := .Values.proxy.services }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $service.name }}
spec:
  ports:
    - port: {{ $service.port }}
      protocol: TCP
      targetPort: {{ $service.port }}
      name: http
{{- end }}
