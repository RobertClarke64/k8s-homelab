apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: convertx
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  refreshInterval: 10s

  target:
    name: convertx

  data:
    - secretKey: JWT_SECRET
      remoteRef:
        key: /convertx/JWT_SECRET
